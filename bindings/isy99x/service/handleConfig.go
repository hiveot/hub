// Package internal handles node configuration commands
package service

// HandleConfigCommand for handling binding, gateway and node configuration changes
// Not yet supported
//func (svc *IsyBinding) HandleConfigCommand(nodeHWID string, config types.NodeAttrMap) {
//	slog.Info("IsyBinding.HandleConfigCommand for node", "HWID", nodeHWID)
// at this moment no ISY configuration is changed so just pass it on
//svc.pub.UpdateNodeConfigValues(nodeHWID, config)
//}
