<!--Thing configuration list-->
<!--@param object of type thing.DetailsTemplateData -->

<ul class="h-grid-table thing-config" striped border>
	<li>
		<div class="h-show-sm">ID</div>
		<div>Name</div>
		<div>Value</div>
		<div class="h-show-lg">Type</div>
		<div class="h-show-md">Default</div>
	</li>

    {{range $k, $v := .Config}}
        {{- /*gotype: github.com/hiveot/hub/lib/things.PropertyAffordance*/ -}}
				<li>
					<div class="h-show-sm"><span>{{$k}}</span></div>
					<div>{{$v.DataSchema.Title}}</div>
					<div>
						<button style="padding: 6px 20px; margin-right: 6px"
						        hx-trigger="click"
						        hx-get="/app/thing/editConfig"
						        hx-vals='{"agentID":"{{$.AgentID}}","thingID":"{{$.ThingID}}","key":"{{$k}}"}'
						        hx-target="#editConfigModal"
						>
							<iconify-icon icon="mdi:pencil"></iconify-icon>
						</button>
              {{printf "%.100s" $v.InitialValue}}  {{$v.DataSchema.Unit}}
					</div>
					<div class="h-show-lg">{{$v.Type}}</div>
					<div class="h-show-md">{{$v.Default}}</div>
				</li>
    {{end}}
</ul>
<div id="editConfigModal"></div>

<style>

    .thing-config {
        overflow-x: auto;
    }

    @media (width < 576px) {
        .thing-config {
            /*xs, show column 2,3 */
            grid-template-columns:
				            minmax(200px, 1fr)
				            minmax(100px, max-content);
        }
    }

    @media (width >= 576px) and (width < 768px) {
        .thing-config {
            /*sm, show column 1,2,3 */
            grid-template-columns:
				            minmax(100px, max-content)
				            minmax(200px, 1fr)
				            minmax(100px, max-content);
        }
    }

    @media (width >= 768px) and (width < 1024px) {
        .thing-config {
            /*md,show column 1,2,3,4*/
            grid-template-columns: minmax(100px, max-content)
              minmax(200px, max-content)
              minmax(100px, 1fr)
              minmax(100px, max-content);
        }
    }

    @media (width >= 1024px) {
        .thing-config {
            /*lg, show all columns*/
            grid-template-columns: minmax(100px, max-content)
              minmax(200px, max-content)
              minmax(100px, max-content)
              100px
              minmax(100px, 1fr);
        }
    }

</style>